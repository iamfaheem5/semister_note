# Assignment - 2

# Function: —

(*) Draw the graph and test whether they are function or not (using vertical line test):

(i) $$j = -x + 2$$ (ii) $$j = -|x| - 3$$ (iii) $$j = -|x+2| + 2$$

(iv) $$j = \sqrt{x-2} + 2$$ (v) $$j = (x+2)^n + 1$$ (vi) $$j = -3x+1$$

(vii) $$x + j = a$$ (viii) $$\frac{x^n}{a^n} + \frac{j^n}{b^n} = 1$$

# Limit: —

(*) — (CT-1 ques):

$$\text{If } f(x) = \begin{cases} e^{-\frac{|x|}{2}}; & 1 \le x \le 0 \\ x^n; & 0 \le x \le 2 \end{cases}$$

Find the limit at $$x = 0$$.

# continuity: —

(*) Define continuous function

(*) (CT-1 ques): If $$f(x) = |x-1|$$, then test the continuity at $$x = 1$$.

# Differentiability: —

(*) (CT-1 ques): If $$f(x)$$ is differentiable at $$x = a$$, then show that function is continuous at that point

(*) If $$f$$ (CT-1 ques): If $$f(x) = \begin{cases} 1, & x < 0 \\ 1 + \sin x, & 0 \le x \le \pi/2 \\ 2 + (x - \frac{a}{2})^n, & x \gg \pi/2 \end{cases}$$

check the Differentiability of $$f(x)$$ at $$x = \frac{a}{2}$$.

# Gamma-Beta function:

(*) Define Gamma and Beta function

(*) (CT-2 ques): show that $$\sqrt{\frac{1}{2}} = \sqrt{\pi}$$.

(*) show that $$\int_{0}^{\pi/2} \sin^p \theta \cdot \cos^q \theta \, d\theta = \frac{\frac{\sqrt{p+1}}{2} \cdot \frac{\sqrt{q+1}}{2}}{2 \cdot \frac{p+q+2}{2}}$$

(*) (CT-2 ques): using Gamma and Beta function evaluate $$\int_{0}^{\pi/2} \sin^x x \cdot \cos^4 x \, dx$$

# Maximum-Minimum:

(*) Find the critical point of $$f(x) = 4x^3 - 9x^2 + 6x$$

(*) Find the inflection point of $$f(x) = \frac{x^3}{3} + ax^2 - 3ax$$

(*) Find the maximum and minimum value of $$f(x) = \frac{4}{x} + \frac{36}{2-x}$$.

(*) (CT-2 ques): Find the maximum and Minimum value of $$f(x) = 2x^3 - 3x^2 - 12x$$.

# Rolle's thm and Mean-value thm:

(*) state Rolle's theorem.

(*) verify Rolle's theorem for $$f(x) = x^2 - 3x + 2$$ in the interval $$(\pm, 2)$$.

(*) verify Rolle's theorem for $$f(x) = (x-1)(x-2)(x-3)$$ in the interval $$(\pm, 3)$$.

(*) State mean-value theorem.

(*) Find the value of c of the mean-value theorem $$f(b) - f(a) = f'(c)(b-a)$$ if $$f(x) = (x-1)(x-2)(x-3)$$ on interval $$(0,4)$$.

# Indeterminant form (L'Hospital Rule):

(*) Define Indeterminant form

(*) State L'Hospital Rule

(*) (CT-2 que): using L'Hospital Rule evaluate $$\lim_{x \to 0} \left[ \frac{1}{x} - \frac{1}{x^n} \log(1+x) \right]$$.

# Area: —

(*) Find the area of a region bounded by parabola $$y = 4x$$ and straight line $$y = 2x$$.

(*) Find the area included between the cycloid $$x = a(\theta + \sin \theta)$$, $$y = a(1 + \cos \theta)$$ and its base.

(*) Find the area included between the cycloid $$x = a(\theta - \sin \theta)$$, $$y = a(1 - \cos \theta)$$ and its base.

# Integration:

(*) Titas → (12·B):

(41) $$\int \frac{dx}{\sqrt{(x-\alpha)(B-x)}}$$

(43) $$\int \frac{3x+2}{5x+2x+3} dx$$

(*) Titas → (12·C):

(53) $$\int \frac{x^3}{\sqrt{x-1}}$$

(57) $$\int \frac{x dx}{(x+1)\sqrt{1+2x-x^2}}$$

# Increasing-Decreasing function and tangent-normal:

(*) Define increasing and decreasing function.

(*) Show that $$f(x) = x^3 - 3x + 3x$$ increases at point $$x = 2$$.

(*) Show that $$f(x) = x^4 - 8x$$ decreases at $$x = 1$$.

(*) Find the exp of tangent line and normal line of $$f = x + 1$$ at point (2,5).

State and proof Fundamental theorem of calculus.

or

State and proof Fundamental theorem of Integral calculus.